HF_TOKEN=hf_xxx

# =1 sau khi ./hf-cache đã có đủ model (start nhanh, không gọi HF)
HF_HUB_OFFLINE=0

# Latency / VRAM knobs (compose → vllm-serve.sh)
# MAX_MODEL_LEN=512
# MAX_LORA_RANK=16
# GPU_MEMORY_UTILIZATION=0.92   # tăng nếu lỗi "No available memory for the cache blocks"
# MAX_NUM_SEQS=4
# ENFORCE_EAGER=0               # =1 nếu vẫn OOM lúc start (chậm hơn lúc chạy)
